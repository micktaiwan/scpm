<p>
	<%= link_to "Stream", :controller => "streams", :action => "show", :id => @stream.id %>-
	<%= link_to "Projects", :controller => "streams", :action => "show_stream_projects", :id => @stream.id %>-
	<%= link_to "Informations", :controller => "streams", :action => "show_stream_informations", :id => @stream.id %>
	<% @stream.get_stream_review_types.each do |review_type| %>
		<%= link_to  review_type.title , :controller => "streams", :action => "show_stream_review", :id => @stream.id, :type => review_type.id %>
	<% end %>
</p>

<h3>Requests</h3>
<ul>
<%= render(:partial=>'projects/request', :collection=>@stream.requests) %>
</ul>