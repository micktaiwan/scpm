<p><%= link_to "Stream", :controller => "streams", :action => "show", :id => @stream.id %></p>
<ul>
<% @projects.each do |project| %>
  <%= render(:partial=>'projects/project', :object=>project) %>
<% end %>
</ul>
