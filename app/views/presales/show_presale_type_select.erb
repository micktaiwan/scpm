<%= render(:file=>"presales/menu") %>

<div id="presale_content">
	<h3>Select a type</h3>	
	<% @presale_types.each do |pt| %>
		<p>
			<%= link_to pt.title , {:controller => "presales", :action => "create_presale_presale_type", :presale_id => @presale.id, :presale_type_id => pt.id },{:class=>"btnlnk"} %>
		</p>
	<% end %>
</div>