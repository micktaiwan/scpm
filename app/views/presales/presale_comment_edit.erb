<h2>Comment for <%= @presale_comment.presale_presale_type.presale_type.title %> <%= @presale_comment.presale_presale_type.milestone_name.title if @presale_comment.presale_presale_type.milestone_name %></h2>

<%= error_messages_for :presale_comment %>
<% form_for @presale_comment, :url=>{:action=>'update_presale_comment', :id=>@presale_comment.id} do |f| -%>
  <%= text_area(:presale_comment, :comment, {:cols=>100, :rows=>7 }) %><br />
  <%= submit_tag 'Update' %></p>
<% end -%>

