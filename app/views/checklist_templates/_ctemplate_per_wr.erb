<% 
	# ctemplate_per_wr_workpackages = ctemplate_per_wr.workpackages.map { |w| w.id }
	if ctemplate_per_wr.workpackages.include? current_wp and ctemplate_per_wr.milestone_names.include? current_m
%>
<li id="template_<%=ctemplate_per_wr.id%>">
  - <%= ctemplate_per_wr.title %>
  <ul>
    <%= render(:partial=>"ctemplate_per_wr", :collection=>ctemplate_per_wr.children, :locals => { :current_wp => current_wp, :current_m => current_m  } ) %>
  </ul>
</li>
<% end %>

