<li id="sdp_task_<%=t.id%>">
  <%= t.title %> (<%= t.initial %>)
  <%= link_to_remote(image_tag("delete.gif"),
   {:url=>{:action=>'unlink_sdp_task', :id=>wl_line.id, :sdp_task_id=>t.sdp_id, :update_sdp_tasks_name=> params[:update_sdp_tasks_name]},
   :confirm=>"Are you sure ?",
   :failure=>"alert('Error:' + request.responseText);"},
   {:class=>"delete"}) %>
</li>
