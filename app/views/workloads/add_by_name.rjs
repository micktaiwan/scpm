if @error
  page.alert @error
else
  page.hide 'wl_line_add_form'
  page.replace "workload", render(:partial=>'workload', :object=>@workload)
  page << "$j('table').fixedHeaderTable({ height: '400', footer: false, fixedColumn: false });"
end

