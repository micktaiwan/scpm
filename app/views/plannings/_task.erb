<li>
  <%= link_to(image_tag('edit.gif'), :controller=>'tasks', :action=>'edit', :id=>t.id) %>
  <%= link_to(t.name) %>
  <%= t.start_date %> => <%= t.end_date %>
  (<%= t.duration_in_day %> days)
<li>
