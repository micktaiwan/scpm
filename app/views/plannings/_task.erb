<li id="task_<%=t.id%>">
  <%= link_to(image_tag('edit.gif'), :controller=>'tasks', :action=>'edit', :id=>t.id) %>
  <%= link_to(t.name) %>
  <%= t.start_date %> => <%= t.end_date %>
  (<%= t.work_in_day %> days => <%= t.team_size %> men)
  <%= link_to(image_tag('delete.gif'), {:controller=>'tasks', :action=>'destroy', :id=>t.id},
  :confirm=>"Are you sure you want to delete this task ?"#,
  #:success=>"$('task_#{t.id}').hide();"
  ) %>
<li>
