<%= link_to("New", {:action=>'new'}) %>
<% @monthlyTaskTypes.each do |mt| %>
	<p><%= mt.name %> <%= link_to("Edit", { :action=>'edit', :id=>mt.id}) %> <%= link_to("Delete", { :action=>'destroy', :id=>mt.id}) %></p>
<% end %>